/**
 * @format
 */

import App from '../App';

describe('App', () => {
  it('should export the App component', () => {
    expect(App).toBeDefined();
    expect(typeof App).toBe('function');
  });
});
